apiVersion: v1
kind: Pod
metadata:
    name: ngnix
    labels:
        app: web
spec:
    containers:
        - name: ngnix
          image: nginx:1.13
          ports:
              - containerPort: 80
